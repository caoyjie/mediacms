## MediaCMS CLI 工具

### 配置和使用

1. 安装 `cli-tool/requirements.txt` 中列出的依赖。
2. 在 `mediacms/cli-tool/` 目录创建 `.env` 文件，并填写工具所需配置。
3. 执行 `python cli.py login` 登录。工具会保存后续认证所需的凭据。
4. 执行 `python cli.py whoami` 检查凭据和当前用户信息。

> 英文原文：[README.md](README.md)
